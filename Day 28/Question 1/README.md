# Question 1 - Dish Count State

A React application that adds interactive state to each dish.

## Features

- Reusable `Dish` component
- Local state with `useState`
- "Add" button for each dish
- Dish count increases when the button is clicked
- Count is displayed beside the dish name

## How to Run

Install dependencies:

```bash
npm install